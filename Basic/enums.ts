enum userrole {
    ADMIN = "admin",
    GUEST = "guest",
    SUPER_ADMIN = "super_admin"
}

enum statuscode {
    ABANDONED = "abandoned status code with 500",
    NOTFOUND = "not found status code 404",
}

statuscode.NOTFOUND